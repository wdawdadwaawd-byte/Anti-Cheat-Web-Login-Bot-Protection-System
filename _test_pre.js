/* build:8d12cae1cbfb0e75 ts:1787329142 v:8.7.1 */
var _dc9f4fcd=(50487+143);
var _dce61dcf6d=(10699-18);
var _dc77b2fabc8c=(34794^248);
var _dc89b434a479ea=(25897|183);
var _dc09aeff91d906=(60633*6);
var _dc2f3ac601cec4=(658&147);
var _dc90d1130a840e=(9573+78);
function _df761e7ea8(_a,_b){return _a*_b;}
function _df3d65e885(_a,_b){return _a+_b;}
if((7*4!==28||(typeof undefined==='number')||([][0]===1))){var _dpa8959a1634='a9e09a2d';}/* dead */
var _skfb1a945cfd='67846a38b5d77d5e';
var _sae8d7b2b9d233=["EvcPGMajDzcE8A==","NMwrFYfiSw==","Tr9gGJX3XX5HpEoYlfddfkekSlHT91V/BPASEZWlGCoS9gQY","XI5gGJX3XX5HpEoYlfddfkekSlvBr1MqAvweetSkGDIO6g8YiPc=","MMU5fOKILjYO4QZc6ifiyvXbUgGH","FeMIWZ3mTWxLpFgIgftdbkukWhaC/g==","MMU5fOKIMD8V7w9MxbscPQI=","BOUETtSkIjsV9gVK","BOUETtSk","AvwaXce+EDsJ8AtUmKAYPADo","Ces1T9C1GjI=","MMEof/mIGTsF8Q1nx7ITOgL2D0rqvhM4CA==","AOEEXce+Hg==","BvEOUdqICDAU8RpI2qUJOwM=","E/YDWduwETs=","BvEOUdqIGCwV6xg=","CeUcUdK2CTEVqh1d17MPNxHhGAXBpQg7","A/YDTtCl","D+ELXNmyDi044hhZ2LIKMRXvNU7UpQ4=","Cu0ZS9y5GgEQ7QRc2qBTPQ/2BVXQ","BvQaVNy0HCoO6wQX37YLPxTnGFHFow==","S45KGJX3XX5HpEoYlfddfkekShiV9xU7BuAPSsbtXSVH","R/lGMpX3XX5HpEoYlfddfkekShiV911+BPYPXNC5CTcG6BkClQ==","D/Q1TMe2DQEB7Q9U0Q==","SOUaUZqkGD0S9gNMzPgLOxXtDEGYtBU/C+gPVtKy","JOsETNC5CXMz/Rpd","BvQaVNy0HCoO6wQX36QSMA==","FOUHXZi4DzcA7QQ=","I+uup8eiET8K5Upa1BLiPxVA20txZgc="];
(function(_sae8d7b2b9d233,30){
  var _sra8493e=function(){
    var _r=_sae8d7b2b9d233.shift();
    _sae8d7b2b9d233.push(_r);
  };
  for(var _i=0;_i<30;_i++){_sra8493e();};
})(_sae8d7b2b9d233,30);
function _sdeb2d99174c(idx){
  var _enc=_sae8d7b2b9d233[idx];
  var _kb=[];
  for(var _i=0;_i<_skfb1a945cfd.length;_i+=2){
    _kb.push(parseInt(_skfb1a945cfd.substr(_i,2),16));
  }
  var _b=atob(_enc);
  var _out='';
  for(var _i=0;_i<_b.length;_i++){
    _out+=String.fromCharCode(_b.charCodeAt(_i)^_kb[_i%_kb.length]);
  }
  return _out;
}
var _psn064aba89c1=false;
var _ftokdfed88='267d6b93353a2bfcd511efdab7f6f0db01367be45be56a3d95b8e692efdaf0fd';
function _ntgd31f4b7c088b(){
  var _snt8351830ce4=[fetch,crypto.subtle.digest,navigator.userAgent];
  try{
    var _fs=Function.prototype.toString.call(fetch);
    var _cs=Function.prototype.toString.call(crypto.subtle.digest);
    if(_fs.indexOf('[native code]')===-1||_cs.indexOf('[native code]')===-1){
      _psn064aba89c1=true;
    }
  }catch(e){}
}
_ntgd31f4b7c088b();
function _hltb9db9d(){
  var _wdc8f132d=(navigator.webdriver===true);
  var _plc97c87ad1=(navigator.plugins&&navigator.plugins.length===0);
  var _wnc1d8d5e5727=(window.chrome===undefined&&/Chrome/.test(navigator.userAgent));
  if(_wdc8f132d||_plc97c87ad1||_wnc1d8d5e5727||_psn064aba89c1){
    _ftokdfed88=_ftokdfed88.split('').reverse().join('')+'03b2b691';
    _psn064aba89c1=true;
  }
}
_hltb9db9d();
var _df83d542e1=false;
function _tg77dda248be30(){
  var _t07fa15b=performance.now();
  (function(){debugger;})();
  var _t18a7cc07dd34e=performance.now();
  if(_t18a7cc07dd34e-_t07fa15b>267){
    _df83d542e1=true;
    (function _fkfb2edb(){
      var _i=0;
      while(_df83d542e1){
        debugger;
        _i++;
        if(_i>457006){_i=0;}
      }
    })();
  }
}
_tg77dda248be30();
if((9*4===36&&(40%2===40%2)&&(typeof String==='function')&&(!![]===true))){/* live */}else{var _dpcf5e8a7e='cece12d9';}
if((7*7===49&&(59%2===59%2)&&(typeof String==='function')&&(!![]===true))){/* live */}else{var _dp37207104b784='8adee72f';}
if((8*5===40&&(14%2===14%2)&&(typeof String==='function')&&(!![]===true))){/* live */}else{var _dpabaa0266='297b8753';}
if((7*6===42&&(19%2===19%2)&&(typeof String==='function')&&(!![]===true))){/* live */}else{var _dpc4320d='85e1dddc';}

/* === CORE === */

/**
 * WASDW Polymorphic Defense Engine & Sensor
 * (Dynamic VM Opcode Solver, Hardware Fingerprint & Ephemeral Challenge Handshake)
 */
(function (window, document) {
    'use strict';

    class WASDSensorEngine {
        constructor() {
            this.startTime = performance.now();
            this.currentTicket = null;
            this.isSolving = false;
        }

        async sha256(str) {
            const buffer = new TextEncoder().encode(str);
            const digest = await crypto.subtle.digest('SHA-256', buffer);
            return Array.from(new Uint8Array(digest)).map(b => b.toString(16).padStart(2, '0')).join('');
        }

        async hmacSha256(keyStr, dataBytes) {
            const keyBuffer = new TextEncoder().encode(keyStr);
            const cryptoKey = await crypto.subtle.importKey(
                'raw',
                keyBuffer,
                { name: 'HMAC', hash: 'SHA-256' },
                false,
                ['sign']
            );
            const signature = await crypto.subtle.sign('HMAC', cryptoKey, dataBytes);
            return Array.from(new Uint8Array(signature)).map(b => b.toString(16).padStart(2, '0')).join('');
        }

        hexToBytes(hex) {
            const bytes = new Uint8Array(hex.length / 2);
            for (let i = 0; i < bytes.length; i++) {
                bytes[i] = parseInt(hex.substr(i * 2, 2), 16);
            }
            return bytes;
        }

        bytesToHex(bytes) {
            return Array.from(bytes).map(b => b.toString(16).padStart(2, '0')).join('');
        }

        // chal_ek (page_nonce ile XOR şifrelenmiş ephemeral key) -> gerçek ephemeral_key
        // Backend'deki encrypt_ephemeral_key ile birebir simetrik (XOR kendi tersidir).
        async decryptEphemeralKey(encryptedHex, pageNonce) {
            const ekBytes = this.hexToBytes(encryptedHex);
            const nonceBuffer = new TextEncoder().encode(pageNonce);
            const digest = await crypto.subtle.digest('SHA-256', nonceBuffer);
            const pad = new Uint8Array(digest).slice(0, ekBytes.length);

            const decrypted = new Uint8Array(ekBytes.length);
            for (let i = 0; i < ekBytes.length; i++) {
                decrypted[i] = ekBytes[i] ^ pad[i];
            }
            return this.bytesToHex(decrypted);
        }

        async getCanvasFingerprint() {
            try {
                const canvas = document.createElement('canvas');
                canvas.width = 240;
                canvas.height = 60;
                const ctx = canvas.getContext('2d');
                if (!ctx) return 'canvas_unsupported';

                ctx.textBaseline = 'top';
                ctx.font = "14px 'Arial', sans-serif";
                ctx.fillStyle = '#f60';
                ctx.fillRect(125, 1, 62, 20);

                ctx.fillStyle = '#069';
                ctx.fillText('WASDW_Shield_🔒_892', 2, 15);
                ctx.fillStyle = 'rgba(102, 204, 0, 0.7)';
                ctx.fillText('WASDW_Marketplace', 4, 35);

                return await this.sha256(canvas.toDataURL());
            } catch (e) {
                return 'canvas_error';
            }
        }

        getWebGLInfo() {
            try {
                const canvas = document.createElement('canvas');
                const gl = canvas.getContext('webgl') || canvas.getContext('experimental-webgl');
                if (!gl) return { vendor: 'no_webgl', renderer: 'no_webgl' };

                const debugInfo = gl.getExtension('WEBGL_debug_renderer_info');
                if (!debugInfo) return { vendor: 'generic', renderer: 'generic' };

                return {
                    vendor: gl.getParameter(debugInfo.UNMASKED_VENDOR_WEBGL) || '',
                    renderer: gl.getParameter(debugInfo.UNMASKED_RENDERER_WEBGL) || ''
                };
            } catch (e) {
                return { vendor: 'error', renderer: 'error' };
            }
        }

        async getAudioFingerprint() {
            try {
                const AudioCtx = window.OfflineAudioContext || window.webkitOfflineAudioContext;
                if (!AudioCtx) return 'audio_unsupported';

                const context = new AudioCtx(1, 44100, 44100);
                const oscillator = context.createOscillator();
                oscillator.type = 'triangle';
                oscillator.frequency.setValueAtTime(10000, context.currentTime);

                const compressor = context.createDynamicsCompressor();
                compressor.threshold.setValueAtTime(-50, context.currentTime);
                compressor.knee.setValueAtTime(40, context.currentTime);
                compressor.ratio.setValueAtTime(12, context.currentTime);
                compressor.attack.setValueAtTime(0, context.currentTime);
                compressor.release.setValueAtTime(0.25, context.currentTime);

                oscillator.connect(compressor);
                compressor.connect(context.destination);
                oscillator.start(0);

                const buffer = await context.startRendering();
                let output = 0;
                const channelData = buffer.getChannelData(0);
                for (let i = 4500; i < 5000; i++) {
                    output += Math.abs(channelData[i]);
                }
                return output.toString();
            } catch (e) {
                return 'audio_error';
            }
        }

        detectAutomationArtifacts() {
            const artifacts = [];
            if (navigator.webdriver) {
                artifacts.push('navigator.webdriver=true');
            }
            const cdcKeys = Object.keys(window).filter(k => k.startsWith('cdc_') || k.startsWith('$cdc_') || k.includes('driver'));
            if (cdcKeys.length > 0) {
                artifacts.push(`selenium_vars:${cdcKeys.join(',')}`);
            }
            if (window.__puppeteer_evaluation_script__ || window.__nightmare || window.callPhantom || window._phantom) {
                artifacts.push('headless_framework_vars');
            }
            if (/Chrome/.test(navigator.userAgent) && !window.chrome) {
                artifacts.push('missing_window.chrome');
            }
            return artifacts;
        }

        solvePoWInWorker(salt, difficulty) {
            return new Promise((resolve) => {
                const workerCode = `
                    self.onmessage = async function(e) {
                        const { salt, difficulty } = e.data;
                        const targetPrefix = '0'.repeat(difficulty);
                        let nonce = 0;
                        const encoder = new TextEncoder();
                        
                        while (true) {
                            const candidate = salt + nonce.toString();
                            const buffer = encoder.encode(candidate);
                            const digest = await crypto.subtle.digest('SHA-256', buffer);
                            const hash = Array.from(new Uint8Array(digest)).map(b => b.toString(16).padStart(2, '0')).join('');
                            
                            if (hash.startsWith(targetPrefix)) {
                                self.postMessage({ solution: nonce.toString(), hash: hash });
                                break;
                            }
                            nonce++;
                        }
                    };
                `;
                const blob = new Blob([workerCode], { type: 'application/javascript' });
                const worker = new Worker(URL.createObjectURL(blob));

                worker.onmessage = (e) => {
                    worker.terminate();
                    resolve(e.data);
                };
                worker.postMessage({ salt, difficulty });
            });
        }

        // Dinamik VM Opcode Matris Dönüştürücüsü
        // hmacKey ARTIK ephemeral_key (backend'deki compute_vm_signature ile birebir aynı)
        async executeVmTransformation(telemetryStr, operations, salt, hmacKey) {
            const encoder = new TextEncoder();
            const dataBytes = new Uint8Array(encoder.encode(telemetryStr));

            for (let i = 0; i < operations.length; i++) {
                const op = operations[i];
                const t = op.op;
                const k = op.k;
                const n = dataBytes.length;

                if (t === 'XOR') {
                    for (let idx = 0; idx < n; idx++) {
                        dataBytes[idx] ^= (k + idx) % 256;
                    }
                } else if (t === 'ADD_MOD') {
                    for (let idx = 0; idx < n; idx++) {
                        dataBytes[idx] = (dataBytes[idx] + k) % 256;
                    }
                } else if (t === 'ROT') {
                    const shift = (k % 7) + 1;
                    for (let idx = 0; idx < n; idx++) {
                        const b = dataBytes[idx];
                        dataBytes[idx] = ((b << shift) | (b >> (8 - shift))) & 0xFF;
                    }
                } else if (t === 'SBOX') {
                    for (let idx = 0; idx < n; idx++) {
                        dataBytes[idx] = (255 - dataBytes[idx] ^ k) & 0xFF;
                    }
                } else if (t === 'SWAP_PAIRS') {
                    for (let idx = 0; idx < n - 1; idx += 2) {
                        const tmp = dataBytes[idx];
                        dataBytes[idx] = dataBytes[idx + 1];
                        dataBytes[idx + 1] = tmp;
                    }
                } else if (t === 'MUL_MOD') {
                    const mk = k | 1;
                    for (let idx = 0; idx < n; idx++) {
                        dataBytes[idx] = (dataBytes[idx] * mk) % 256;
                    }
                } else if (t === 'FOLD_XOR') {
                    for (let idx = 0; idx < Math.floor(n / 2); idx++) {
                        dataBytes[idx] ^= dataBytes[n - 1 - idx];
                    }
                } else if (t === 'CASCADE') {
                    for (let idx = 1; idx < n; idx++) {
                        dataBytes[idx] ^= dataBytes[idx - 1];
                    }
                }
            }

            // Ephemeral key + salt ile HMAC-SHA256 imzala (statik anahtar YOK artık)
            const secretSaltKey = `${hmacKey}:${salt}`;
            return await this.hmacSha256(secretSaltKey, dataBytes);
        }

        async performSecurityHandshake() {
            if (this.isSolving) return false;
            this.isSolving = true;

            try {
                // Adım 1: Challenge Al
                const chalUrl = '/api/security/challenge' + window.location.search;
                const chalRes = await fetch(chalUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    credentials: 'same-origin'
                });

                if (!chalRes.ok) {
                    const errJson = await chalRes.json().catch(() => ({}));
                    throw new Error(errJson.message || errJson.error || 'Challenge alınamadı');
                }

                const chalData = await chalRes.json();

                // Adım 1b: Ephemeral key'i page_nonce ile çöz
                // page_nonce yalnızca gerçek sayfa yüklemesinde HTML'e basılır (window.__WASDW_PAGE_NONCE)
                const pageNonce = window.__WASDW_PAGE_NONCE;
                if (!pageNonce) {
                    throw new Error('Sayfa nonce değeri bulunamadı (sayfa doğru yüklenmemiş olabilir)');
                }
                const ephemeralKey = await this.decryptEphemeralKey(chalData.chal_ek, pageNonce);

                // Adım 2: WebWorker ile PoW çöz
                const t0 = performance.now();
                const powResult = await this.solvePoWInWorker(chalData.chal_salt, chalData.chal_diff);
                const solveTimeMs = Math.round(performance.now() - t0);

                // Adım 3: Telemetri ve donanım hash'lerini topla
                const canvasHash = await this.getCanvasFingerprint();
                const audioHash = await this.getAudioFingerprint();
                const webgl = this.getWebGLInfo();
                const automation = this.detectAutomationArtifacts();

                const hpElem = document.getElementById('hp_trap_field');
                const hpValue = hpElem ? hpElem.value : '';
                const dwellTimeMs = Math.round(performance.now() - this.startTime);

                // Adım 4: Telemetri Özetini Dinamik VM Matrisinden Geçirip Ephemeral Key ile İmzala
                const telemetrySummary = `${canvasHash}:${audioHash}:${webgl.vendor}:${webgl.renderer}:${powResult.solution}`;
                const sensorSignature = await this.executeVmTransformation(
                    telemetrySummary,
                    chalData.chal_ops,
                    chalData.chal_salt,
                    ephemeralKey
                );

                // Adım 5: Callback Gönder
                const verifyPayload = {
                    challenge_token: chalData.chal_payload,
                    solution_nonce: powResult.solution,
                    sensor_signature: sensorSignature,
                    telemetry_summary: telemetrySummary,
                    canvas_hash: canvasHash,
                    audio_hash: audioHash,
                    webgl_vendor: webgl.vendor,
                    webgl_renderer: webgl.renderer,
                    webdriver: !!navigator.webdriver,
                    automation_artifacts: automation,
                    plugins_len: navigator.plugins ? navigator.plugins.length : 0,
                    screen_w: window.screen.width,
                    screen_h: window.screen.height,
                    color_depth: window.screen.colorDepth,
                    dwell_time_ms: dwellTimeMs,
                    hp_field: hpValue
                };

                const verifyUrl = '/api/security/verify-challenge' + window.location.search;
                const verifyRes = await fetch(verifyUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    credentials: 'same-origin',
                    body: JSON.stringify(verifyPayload)
                });

                const verifyData = await verifyRes.json();
                this.isSolving = false;

                if (verifyRes.ok && verifyData.status === 'ok') {
                    this.currentTicket = verifyData.login_ticket;
                    return { success: true, ticket: verifyData.login_ticket, solveTimeMs };
                } else {
                    return { success: false, reason: verifyData.message || 'Doğrulama başarısız' };
                }
            } catch (err) {
                this.isSolving = false;
                return { success: false, reason: err.message };
            }
        }
    }

    window.ShieldCore = new WASDSensorEngine();

})(window, document);